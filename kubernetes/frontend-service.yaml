apiVersion: v1
kind: Service

metadata:
  name: food-frontend-service

spec:
  selector:
    app: food-frontend

  ports:
    - protocol: TCP

      port: 80

      targetPort: 80

      nodePort: 30080

  type: NodePort
