apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mongodb-storage
  namespace: food-app

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: local-storage

  resources:
    requests:
      storage: 2Gi
